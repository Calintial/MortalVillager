Ubuntu (12.04 or 13.10)
Install :
	Irrlicht (Version 1.8):
		sudo apt-get install libirrlicht-dev # 13.10
		
	Fmod (Version 4.44.26) :
		Download fmod (http://www.fmod.org)
		Archivo fmodapi44426linux.tar.gz
		cp [...]api/inc/* /usr/include/fmodex/
		cp [...]api/lib/* /usr/lib/
		64x:
			Rename libfmod64.so -> libfmod.so

	Boost:
		sudo apt-get install libboost1.53-dev(libbostX.X-dev)

Run:
	./game.sh

Gráfica_Aprendizaje:
	Compilación:
		make
	Ejecución:
		Copiar el archivo Excel.txt que se quiera mostrar a la carpeta bin
		./main
